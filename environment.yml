name: textmining
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - gigachat
      - python-docx
      - pydantic>=2.0
      - click
      - python-dotenv
      - tqdm
      - tenacity
      - olefile
      - striprtf
