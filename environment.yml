name: textmining
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - hdbscan
  - numpy
  - scipy
  - matplotlib
  - pandas
  - scikit-learn
  - pip:
      - -r requirements.txt
      - python-docx
      - pydantic>=2.0
      - click
      - tenacity
      - olefile
      - striprtf
